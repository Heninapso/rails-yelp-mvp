<%= simple_form_for [@restaurant, @review], :html=> {class: 'container'} do |f| %>
  <%= f.input :rating, hint: 'Give a rating from 0 to 5' %>
  <%= f.input :content %>
  <%= f.button :submit, :class => 'btn-success' %>
<% end %>
